---
worktree: feature/epic-db-migration
context_estimate: S
dependencies: 002
---
# Task 003: yoke-db.sh router: wire new yoke-db.sh epic subcommands

## Description
Update `yoke-db.sh` to route all new `yoke-db.sh epic` subcommands through the `epic` domain. The router already handles `epic` as a domain (dispatching to `yoke-db.sh epic`), so this task verifies the passthrough works for all new subcommands and updates the help text.

Find the `epic)` case in `yoke-db.sh`'s dispatch block and ensure it passes all arguments through to `yoke-db.sh epic`. Update the `cmd_help()` function to list all new epic subcommands.

## Acceptance Criteria
- [ ] AC-1: `yoke-db.sh epic task-update-body my-epic 1 < body.txt` works identically to `yoke-db.sh epic task-update-body my-epic 1 < body.txt`.
- [ ] AC-2: `yoke-db.sh epic dispatch-chain-get my-epic feature/auth` works correctly.
- [ ] AC-3: `yoke-db.sh help epic` lists all new subcommands.
- [ ] AC-4: Existing `yoke-db.sh epic task-list` and other existing subcommands still work.

## Test Plan
- Unit tests: Run each new subcommand via `yoke-db.sh epic` and verify output matches direct `yoke-db.sh epic` invocation.
- Manual verification: `yoke-db.sh help epic` shows updated subcommand list.

## Interface Contract -- Provides
- Module at `.claude/skills/yoke/scripts/yoke-db.sh`
  - Exports: `epic` domain routing for all new subcommands
  - Behavior: Transparent passthrough to `yoke-db.sh epic`

## Interface Contract -- Expects
- Module from task 002 at `.claude/skills/yoke/scripts/yoke-db.sh epic`
  - Must export: all new subcommands

## Documentation Requirements
- **New docs:** none
- **Update docs:** none yet

## Files Touched
- `.claude/skills/yoke/scripts/yoke-db.sh` (modify) -- update help text for epic domain, verify passthrough
