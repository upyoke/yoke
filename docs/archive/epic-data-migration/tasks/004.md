---
worktree: feature/epic-db-migration
context_estimate: M
dependencies: 002
---
# Task 004: Test suite: test-yoke-db.sh epic for new subcommands

## Description
Extend the existing `test-yoke-db.sh epic` test suite to cover all new subcommands added in task 002. The test creates an isolated test DB (using `setup_test_repo` from `test-helpers.sh`), runs `sprint-db.sh init` to create schema, then exercises each subcommand.

Test coverage includes: body round-trip (text with special characters, multi-line markdown), history insertion and retrieval, dispatch chain upsert/get/update/list, review insert/get with both PASS and FAIL verdicts, progress note lifecycle (insert, list-unsynced, mark-synced), simulation upsert/get for both plan and integration phases, and task-update-field for individual column updates.

## Acceptance Criteria
- [ ] AC-1: All existing test-yoke-db.sh epic tests still pass.
- [ ] AC-2: Body round-trip test: markdown with backticks, quotes, newlines stored and retrieved identically.
- [ ] AC-3: History insertion test: multiple entries for same task, retrieved in order.
- [ ] AC-4: Dispatch chain lifecycle: upsert, get, update field, list multiple chains.
- [ ] AC-5: Review lifecycle: insert PASS and FAIL reviews, get by epic/task.
- [ ] AC-6: Progress note lifecycle: insert, list unsynced, mark synced, verify list-unsynced excludes synced.
- [ ] AC-7: Simulation lifecycle: upsert plan phase, upsert integration phase, get each, verify upsert replaces.
- [ ] AC-8: Test passes on both macOS (BSD) and Linux (GNU).

## Test Plan
- This task IS the test suite. Run via `sh test-yoke-db.sh epic`.

## Interface Contract -- Provides
- Module at `.claude/skills/yoke/scripts/tests/test-yoke-db.sh epic`
  - Exports: comprehensive test coverage for `yoke-db.sh epic`

## Interface Contract -- Expects
- Module from task 002 at `.claude/skills/yoke/scripts/yoke-db.sh epic`
  - Must export: all new subcommands

## Documentation Requirements
- **New docs:** none
- **Update docs:** none

## Files Touched
- `.claude/skills/yoke/scripts/tests/test-yoke-db.sh epic` (modify) -- add test cases for all new subcommands
