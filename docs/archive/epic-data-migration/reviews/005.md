# Validation Report: Task 005 — update-status.sh: DB-native rewrite with backward-compatible shim

## Result: PASS

## Acceptance Criteria
- [x] AC-1: PASS — `update-status.sh my-epic 001 in_progress "Started"` updates `epic_tasks` row AND writes JSON file. Verified by Test 19: both DB status and JSON status confirmed as `in_progress`.
- [x] AC-2: PASS — `update-status.sh yoke/epics/my-epic/status/001.json in_progress "Started"` still works. Verified by Test 22: legacy path-based interface updates JSON and DB, and inserts history row.
- [x] AC-3: PASS — History row inserted in `epic_task_history` on every status change. Verified by Test 20: from_status, to_status, and note all stored correctly.
- [x] AC-4: PASS — `dispatch_attempts` incremented when transitioning to `in_progress`. Verified by Test 21: starts at 1, becomes 2 on `in_progress`; no increment on `completed`.
- [x] AC-5: PASS — Auto-unblock queries `epic_tasks` for blocked tasks. Verified by Test 23: completing task 1 unblocks task 2 in DB and prints "Auto-unblocking" message.
- [x] AC-6: PASS — GitHub issue label swap and comment posting still work. Verified by Test 24: `--add-label status:in-progress` and `issue comment` both appear in gh log via DB-native call.
- [x] AC-7: PASS — `update_epic_checkbox()` reads parent item's `github_issue` from DB. Verified by Test 25: `gh issue view 200` was called (the DB-sourced parent issue number).

## Tests
- **Test suite (test-update-status.sh):** PASS — 61 passed, 0 failed
- **Test suite (test-yoke-db.sh epic):** PASS — 120 passed, 0 failed
- **New tests present:** Yes — Tests 19-25 (7 new tests covering all 7 ACs)
- **Coverage notes:** All 7 ACs have dedicated tests. Legacy path-based interface also tested for DB dual-write (Test 22). Auto-unblock from DB tested end-to-end (Test 23).

## Interface Contracts
- **Provides:** PASS — Module exists at `.claude/skills/yoke/scripts/update-status.sh`. Exports dual interface:
  - DB-native: `update-status.sh <epic-slug> <task-num> <new-status> [note]`
  - Legacy path: `update-status.sh <path/to/status/NNN.json> <new-status> [note]`
  - Detection heuristic: `case "$1" in */status/*.json) _legacy=1 ;; *) _legacy=0 ;; esac` (documented in comment block at line 228)
- **Expects (task 001 schema):** VERIFIED — Tests create `epic_tasks` and `epic_task_history` tables with the expected columns; all queries succeed.
- **Expects (task 002 yoke-db.sh epic subcommands):** VERIFIED — `task-update-field`, `history-insert`, `task-get`, `task-list` all present in yoke-db.sh epic and called correctly.

## Documentation
- **New docs created:** N/A — Task spec had no documentation requirements.
- **Existing docs updated:** N/A

## Code Quality
- Follows project conventions: Yes — POSIX sh, no bashisms, no `sed -i`, PID-suffixed temp files (`${STATUS_FILE}.tmp.$$`), proper trap cleanup.
- Security concerns: None. SQL injection mitigation present via `_escape()` function and python3 parameterized queries in yoke-db.sh epic. The three direct `sqlite3 "$YOKE_DB"` calls (lines 326, 603, 690) use `$YOKE_DB` variable (not hardcoded path), which complies with CLAUDE.md's "never hardcode DB paths" rule. The lint hook only blocks agents from typing sqlite3 interactively, not scripts from using it internally.
- Performance concerns: None. DB queries are simple primary-key lookups. Dual-write adds one extra file write per transition, which is acceptable for the transition period.

## Path Tracing
- **Export accuracy:** VERIFIED — Both interfaces work correctly. Path-detection heuristic correctly distinguishes `*/status/*.json` paths from epic-slug arguments. The EPIC_SLUG and TASK_NUM are correctly parsed from legacy paths (`basename` and `dirname` chain on line 252-254). `task_num` passed to `yoke-db.sh epic` as a string (e.g., "003") and the DB accepts it (sqlite3 integer coercion handles leading zeros).
- **Runtime assumptions:** YOKE_ROOT env var is the primary DB resolution path used in tests. When absent, falls back to CLAUDE_PROJECT_DIR, then git root. Worktree stripping (`sed 's|/\.worktrees/.*||'`) ensures the main repo's DB is targeted when called from a worktree. This is consistent with how other scripts behave.
- **Downstream compatibility:** VERIFIED — Tasks 009, 012, 013, and 018 all expect the DB-native interface `update-status.sh <epic-slug> <task-num> <new-status> [note]` which is exactly what was implemented. No downstream breakage expected.

## Issues Found
None. All acceptance criteria verified, all tests pass, worktree is clean, no unexpected artifacts.

## Recommendation
PASS — ready for merge

**VERDICT: PASS**
