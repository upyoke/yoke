# Validation Report: Task 001 — Schema extensions: new tables + ALTER TABLE epic_tasks

## Result: PASS

## Acceptance Criteria
- [x] AC-1: PASS — `epic_task_history` table exists with all 7 required columns (id, epic_slug, task_num, timestamp, from_status, to_status, note). Index `idx_epic_task_history_slug_num` on (epic_slug, task_num) exists. Tested with live insert.
- [x] AC-2: PASS — `epic_dispatch_chains` table exists with all 12 columns. Defaults verified (current_index=0, current_attempt=1, max_attempts=5, no_chain=0). UNIQUE(epic_slug, worktree) constraint enforced.
- [x] AC-3: PASS — `epic_reviews` table exists with all 6 required columns. Index `idx_epic_reviews_slug_num` on (epic_slug, task_num) exists. CHECK constraint on verdict (`IN ('PASS','FAIL')`) verified — rejects 'MAYBE'.
- [x] AC-4: PASS — `epic_progress_notes` table exists with all 8 required columns. synced_to_github DEFAULT 0 verified. UNIQUE(epic_slug, task_num, note_num) constraint enforced.
- [x] AC-5: PASS — `epic_simulations` table exists with all 6 required columns. UNIQUE(epic_slug, phase) enforced. phase CHECK constraint (`IN ('plan','integration')`) verified — rejects 'invalid'.
- [x] AC-6: PASS — `epic_tasks` has all 8 new columns: body TEXT, github_issue TEXT, branch TEXT, worktree_path TEXT, blocked_by TEXT, max_attempts INTEGER DEFAULT 5, agent_id TEXT, last_heartbeat TEXT. Types and defaults verified via pragma_table_info.
- [x] AC-7: PASS — Running `sprint-db.sh init` three times in a row (tested in Test 10) exits 0 each time. Tables and columns remain stable with exactly 17 columns on epic_tasks.
- [x] AC-8: PASS — Existing epic_tasks rows (7) are preserved after migration from old schema. Titles, statuses, and worktrees verified field-by-field. max_attempts defaults to 5 for pre-existing rows; body is NULL for pre-existing rows.

## Tests
- **Test suite (test-schema-extensions.sh):** PASS — 135 passed, 0 failed (11 test cases)
- **Regression suite (test-sprint-db.sh):** PASS — 51 passed, 0 failed
- **Regression suite (test-yoke-db.sh epic):** PASS — 39 passed, 0 failed
- **New tests present:** Yes — 655-line test file with full coverage of all 8 ACs
- **Coverage notes:** Tests cover schema presence, column types, defaults, constraints, idempotency, data preservation, usability of new columns after migration, and column count stability.

## Interface Contracts
- **Provides:** PASS
  - `sprint-db.sh` at `.claude/skills/yoke/scripts/sprint-db.sh` — confirmed.
  - `_add_column_if_not_exists()` function defined at line 50, uses pragma_table_info for idempotent guard.
  - `cmd_init()` extended to create 5 new tables (IF NOT EXISTS) and add 8 columns to epic_tasks.
  - All new tables verified: `epic_task_history`, `epic_dispatch_chains`, `epic_reviews`, `epic_progress_notes`, `epic_simulations`.
  - All 8 new columns on epic_tasks verified with correct types and defaults.
- **Expects:** N/A (first task, no upstream dependencies)

## Documentation
- **New docs created:** No — task spec (001.md) explicitly states "New docs: none (db-reference.md updated in task 024)"
- **Existing docs updated:** No — task spec explicitly states "Update docs: none yet"
- **Progress note written:** Yes — engineer created `yoke/epics/epic-data-migration/updates/001/001-schema-extensions.md`

## Code Quality
- Follows project conventions: Yes
  - POSIX sh shebang (`#!/usr/bin/env sh`), no bashisms
  - No hardcoded DB paths — uses `$YOKE_DB` throughout
  - `_add_column_if_not_exists` uses local-scope variables prefixed with `_acife_` to avoid collisions
  - `pragma_table_info` used correctly for portable SQLite column introspection
- Security concerns: None — no user-provided inputs to SQL in the new helper beyond table/column names passed from hardcoded call sites
- Performance concerns: None — 8 sequential pragma queries on init is negligible

## Path Tracing
- **Export accuracy:** VERIFIED — The interface contract says "6 new tables" in the description but only 5 are named in both the Provides spec and the AC list. The implementation creates exactly the 5 named tables. The "6" in the prose description appears to be an Architect-level counting error in the task spec; the operative list of tables in the contract enumerates 5. The test file (line 605) explicitly acknowledges this discrepancy. No downstream tasks reference a 6th unnamed table, so this does not affect downstream compatibility.
- **Runtime assumptions:** `$YOKE_DB` must be set or resolvable via `_resolve_root()` — this is the same assumption the existing codebase depends on; no new assumption introduced.
- **Downstream compatibility:** VERIFIED — Tasks 002, 005, 015, 016, 017, 018, 019, 021 all expect the tables and columns listed in the interface contract. All 5 tables and all 8 new epic_tasks columns are present and correctly typed.

## Issues Found
1. Task description says "Add 6 new tables" but only 5 are created (minor, informational). The interface contract names only 5 tables and all 5 are implemented. The test suite acknowledges this. No downstream task depends on a 6th table. Not a blocker — the operative contract (Provides section) is satisfied.

## Recommendation
PASS — ready for merge. All 8 acceptance criteria verified by automated tests. All three test suites pass (225 total assertions). Worktree is clean. Interface contract is fully satisfied for downstream tasks.

**VERDICT: PASS**
