# Workflows

Operator documentation moved to the public corpus:

- [docs/public/workflows.md](public/workflows.md)
- Lifecycle detail: [docs/public/reference/lifecycle.md](public/reference/lifecycle.md)

Create a Blitz with `/yoke idea --workflow blitz "{title}"` (`entry_surface:
harness_skill`). Refinement must link exactly one execution strategy document
before `/yoke blitz` begins. After install, the same pages live at
`.yoke/docs/workflows.md` and `.yoke/docs/reference/lifecycle.md`.
