# Yoke 0.1.1+launch.27

- Treat the hosted Platform applications as the production and staging control-plane
  endpoints while retaining the existing package distribution hosts.
- Validate universe archives locally before upload, including schema and table-data
  catalog compatibility, so obsolete or incompatible dumps fail before transfer.
- Enforce control-plane authorization independently from project-scoped permissions.
- Make local GitHub connection behavior explicit and keep hosted GitHub identity and
  installation ownership on the hosted control plane.
- Give runner fleets one direct GitHub App authority in capability settings and reject
  stale selector fields instead of resolving credentials through environment records.
- Regenerate the function Atlas, agent packets, install bundle, and operator guidance for
  the hosted-control-plane and runner authority model.
