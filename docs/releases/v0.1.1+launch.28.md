# Yoke 0.1.1+launch.28

- Trust the exact branch and deployment-environment GitHub OIDC subjects used by
  Platform delivery workflows without widening repository authority.
- Replace the canonical, packaged-universe, and webapp-template wordmark and OG
  assets with the custom ball-lowercase logotype.
- Validate typed project capability documents while an uploaded universe is still
  in its isolated staging database, before any hosted tenant cutover.
