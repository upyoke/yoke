# Yoke 0.1.1+launch.30

- Prove PostgreSQL credential retirement from the authenticated live role
  catalog and serialize cluster-global role tests safely under xdist.
- Move runner-fleet provider-token minting from the hosted tenant to the
  capability-owned AWS broker, while keeping GitHub Actions explicitly denied
  all GitHub App private-key values.
- Bind the broker to one stable function name and exact repository/App
  authority, with read-only preview grants for CI and write-scoped grants only
  for attended local applies.
