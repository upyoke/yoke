# Yoke 0.1.1+launch.29

- Add authenticated, compare-and-swap project capability settings reads and
  writes over the ordinary HTTPS function surface, including typed validation
  and cross-project authorization guards.
- Add an attended source-authority quiesce and export boundary with stable
  whole-authority receipts, bounded-memory content digests, and explicit
  recovery from a database-enforced write freeze.
- Keep environment-owned capability secrets out of portable universe archives
  while recording secret-free type/project evidence for destination overlays.
- Add owner-scoped machine connection and GitHub Actions configuration
  retirement surfaces for post-migration cleanup.
- Align the local universe top bar with the hosted application frame and retain
  the database-backed strategy activity visualization.
