"""Shared command-line entry points for the ``runtime.api`` control plane.

Public entry points:
    - :mod:`yoke_core.cli.raw_query` — Python owner for the
      ``db_router query`` escape hatch
    - :mod:`yoke_core.cli.db_router` — unified DB router and CLI
      for all Yoke domain operations
"""
