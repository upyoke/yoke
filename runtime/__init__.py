# yoke — state directory and Python package root.
#
# This __init__.py makes 'yoke' importable as a Python package so runtime
# modules can use installed ``yoke_core`` domain imports without path hacks.
